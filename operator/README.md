# Backend #

```bash
# start in production mode
yarn start

# start in watch mode
yarn dev

# build production
yarn build

# lint
yarn lint

# autofix lint errors
yarn fix:lint

# test
yarn test

# build docker image
yarn build:docker

# start docker image
yarn start:docker

# pause docker image
yarn pause:docker

# stop docker image
yarn stop:docker
```
